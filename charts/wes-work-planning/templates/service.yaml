apiVersion: v1
kind: Service
metadata:
  name: {{ include "wes-work-planning.fullname" . }}
  labels:
    {{- include "wes-work-planning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wes-work-planning.selectorLabels" . | nindent 4 }}
