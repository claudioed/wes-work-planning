/** Local-dev base URL for wes-work-planning's own REST API. Mirrors
 *  e2e-tests/env.sh's WES_HTTP_PORT=8083. See warehouse-console's
 *  src/config.ts for the note on swapping to runtime config before
 *  multi-environment deployment. */
export const WES_API_BASE = "http://localhost:8083";
