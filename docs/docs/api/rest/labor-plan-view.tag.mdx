---
id: labor-plan-view
title: "Labor Plan View"
description: "Labor Plan View"
custom_edit_url: null
---



A read-only projection of the labor plan Workforce Management last reported for a path (via its own `ShiftPlanCommitted` integration event). NOT this service's own Shift Plan aggregate — same term, different bounded context.




```mdx-code-block
import DocCardList from '@theme/DocCardList';
import {useCurrentSidebarCategory} from '@docusaurus/theme-common';

<DocCardList items={useCurrentSidebarCategory().items}/>
```
      